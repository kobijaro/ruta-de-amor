import React from 'react';
import ReactDOM from 'react-dom/client';
import PassengerTaskManager from './PassengerTaskManager';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PassengerTaskManager />
  </React.StrictMode>
);
